name = "darasaone-upload-worker"
main = "src/index.ts"
compatibility_date = "2024-11-01"

# Secrets (never put these here — set via `wrangler secret put <NAME>`):
#   R2_ACCOUNT_ID
#   R2_ACCESS_KEY_ID
#   R2_SECRET_ACCESS_KEY
#   R2_BUCKET_NAME
#   R2_PUBLIC_BASE_URL
#   SUPABASE_URL
#   SUPABASE_ANON_KEY
#   ALLOWED_ORIGIN
#
# See README.md in this folder for the full deploy walkthrough.
